## 禁止事項 (Safety Rules)
- システムファイル (/etc, /usr, /System 等) へのアクセスや変更は一切禁止。
- 外部への大量のデータ送信、未知のドメインへの通信を伴うコードの生成は禁止。
- 既存のファイルを削除する場合は、必ず事前にユーザーの承諾を得ること。
- シェルコマンドを実行する際は、読み取り専用 (read-only) を優先し、書き換えを伴う場合は確認を求めること。

